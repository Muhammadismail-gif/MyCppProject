#include <iostream>   //A Library It allows us to use input/output

using namespace std;  // This tells C++: “I want to use standard tools easily”
                     //Without this, you must write std::cout

int main()            // starting point of the program
 {   
                    // Every C++ program must start here
                    //  curly brackets They define the body (area) of the program
    cout << "Assalam-o-Alaikum";  // Prints message on screen
    
    return 0;        // Ends program successfully
}